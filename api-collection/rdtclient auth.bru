meta {
  name: rdtclient auth
  type: http
  seq: 1
}

post {
  url: {{host}}/Api/Authentication/Login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "{{username}}",
    "password": "{{password}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
